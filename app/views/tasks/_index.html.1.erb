<h1>課題一覧</h1>

<ul>
  
  <% tasks.each do |task| %>
    
  <li>
    <%= link_to task.id, task %>
    
    <%= task.status %>
    &gt;
    <%= task.content %>
  </li>
  
  <% end %>

</ul>

<!--%= paginate @tasks %> -->

<%= link_to '新規投稿', new_task_path %> 
